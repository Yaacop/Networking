5. 
An IP Address uniquely identifies a device inside a network - Each device must have an unique IP Address.
Duplicate IP means:

Two devices are using the same IP address in the same network. Duplicate IP causes ARP conflicts, Network instability, Packet misdelivery
Symptoms include in a real network you may see:

• Internet suddenly stops
• Ping works sometimes
• Network disconnects randomly

